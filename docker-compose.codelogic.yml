# CodeLogic scans are run from the host with scripts/codelogic/run-dotnet-scan.sh and run-sql-scan.sh
# so the official image ENTRYPOINT is preserved (same as CI). This file is intentionally empty.
#
# Older versions of this repo used Compose services with a custom entrypoint; that breaks the
# agent’s normal "analyze" launcher.
services: {}
